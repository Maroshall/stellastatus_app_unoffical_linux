# 버전 안내

스텔라상태(StellaStatus)의 버전에는 **정식 버전**과 **베타 버전** 두 가지가 있어요.
이 문서는 각 버전이 무엇이고, 베타 버전을 어떻게 받는지 알려드립니다.

## 정식 버전과 베타 버전

### 정식 버전 (Latest)
- 예) `v1.0.5` 처럼 숫자가 **세 칸**이에요.
- 충분히 다듬어진 **안정 버전**으로, 모든 분께 권장하는 기본 버전이에요.

### 베타 버전 (Beta)
- 예) `v1.0.5.1` 처럼 정식 버전 뒤에 **숫자가 한 칸 더** 붙어요. (`.1` ~ `.9`)
- 정식으로 내기 전에 **새 기능을 먼저 써보는 시험판**이에요.
- 최신 기능을 빨리 만나볼 수 있지만, **아직 다듬는 중이라 불안정할 수 있어요.**

> 버전 크기: `v1.0.5` → `v1.0.5.1` → `v1.0.5.2` → … → `v1.0.6`
> 베타(`v1.0.5.1`)는 그 정식(`v1.0.5`)보다 새 버전이고, 다음 정식(`v1.0.6`)이 나오면 정식이 더 최신이 돼요.

## 내 버전 확인하기

앱에서 **설정 → 정보** 를 열면 현재 버전이 보여요. (예: `버전 v1.0.5`)
버그를 신고할 때는 **정보 → "시스템 정보 복사"** 버튼을 누르면 버전·채널 정보가 한 번에 복사돼요.

## 베타 버전 받는 방법

1. 앱에서 **설정 → 일반** 을 엽니다.
2. **"베타 버전 받기"** 를 켭니다.
3. 잠시 뒤 새 베타가 있으면 업데이트 안내가 떠요. 안내에 따라 설치하면 됩니다.

| "베타 버전 받기" | 받는 버전 |
| --- | --- |
| **꺼짐 (기본값)** | 정식 버전만 받아요. 베타는 안내되지 않아요. |
| **켜짐** | 정식·베타 중 **가장 최신 버전**을 받아요. 베타가 있으면 베타를 먼저 받아요. |

## 자주 묻는 질문

**Q. 베타를 쓰다가 정식으로 돌아가려면?**
"베타 버전 받기" 를 끄면 더 이상 새 베타는 받지 않아요. 다만 이미 설치된 베타가 자동으로 정식으로
내려가지는 않습니다. 정식으로 돌아가려면, 홈페이지(GitHub Releases)에서 정식 버전을 내려받아
다시 설치해 주세요.

**Q. 베타가 불안정하면 어떻게 하나요?**
불편한 점이 있으면 **설정 → 정보 → 문제 신고** 로 알려주세요. 문제가 심하면 위 방법으로
정식 버전을 다시 설치하시면 됩니다.

**Q. 꼭 베타를 써야 하나요?**
아니에요. 안정적으로 쓰고 싶다면 **끈 상태(정식 버전)** 를 그대로 두시면 됩니다.
